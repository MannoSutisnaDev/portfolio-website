export enum PageKeys {
  Home = "/",
  Experience = "/experience",
  Education = "/education",
  Projects = "/projects",
  Skills = "/skills",
  AbountMe = "/about-me",
}

export interface ContentBlockType {
  main: string;
  after: string;
  sub: string;
  content: string;
}
